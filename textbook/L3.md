# Kairos 3 級教本 — 読解 / Reading Kairos (Grade 3 Textbook)

**版**: v0（2026-07-30・言語 RC5 準拠）。3 級のねらい＝**Kairos 式を受け取って、何が発火するかを
正確に読めるようになる**。正本は常に[言語仕様](https://kairos-lang.org/spec/)——本書は読解の
道筋を示すだけで、仕様を再定義しない。各章末の演習は本リポジトリのサンプル問題。

## 第 1 章 二層構造——どこに何が書いてあるか

Kairos のソースは二層でできている。**premise 層**（前提の供給）と**本体層**（スケジュールを
編む式）だ。

```kairos
premise JP {                 # ← ここから premise 層:
  calendar-system: Gregorian #    暦法・タイムゾーン・週の始まりなど
  tz: "Asia/Tokyo"           #    「式の解釈が成り立つ条件」を並べる
  wkst: Mon
}

@JP                          # ← 前提の束を指名して……
everyDay |> within(month) |> first   # ← ここが本体層: 供給された語彙で編む一行
```

読解の第一歩は「**この式はどの前提の下で読まれているか**」の確認。同じ式でも前提が変われば
意味が変わる（第 4 章で実例を見る）。契約書の前文と本文の関係と同型——前文を読まずに本文を
読んではいけない。

- 参照: [仕様 §2.1（二層構造）](https://kairos-lang.org/spec/10-types.html)・[§3.1（premise）](https://kairos-lang.org/spec/20-premise-layer.html)
- **演習**: [L3-008 二層構造](../L3/008-two-layers.md)

## 第 2 章 字句とリテラル——書ける形は決まっている

- **日付**: `YYYY-MM-DD` の完全形のみ。**実在する日付だけが書ける**——`2026-02-30` は字句エラーで、
  ロールオーバー（3 月 2 日への読み替え）はしない。「書かれた日付は実在する」を言語が保証する。
- **幅**: `1d`（市民日）・`2h30m`（経過時間）。市民時と経過時間は混ぜられない（`1d12h` はエラー）。
- **文字列**: `"Asia/Tokyo"` ——tz や出所の値に使う。
- **範囲糖衣**: `2026-01-30..2026-02-02` はテーブル要素位置では連続日 4 日に展開される。

読解のコツ: 見慣れない並びを見たら「これは日付か・数値式か」を §5.5 の字句規則で確かめる。
たとえば `2026-01` は日付ではなく**数値の引き算**（2026−1）に読まれる。

- 参照: [仕様 §5.5（字句）](https://kairos-lang.org/spec/40-grammar.html)
- **演習**: [L3-007 日付リテラルの字句](../L3/007-date-lexis.md)

## 第 3 章 生成子・窓・選択子——式の背骨

本体式の基本形は「**生成 → 窓で切る → 選ぶ**」のパイプライン:

```kairos
everyDay |> within(month) |> first
```

- `everyDay`——暦法純粋な日刻みの列（生成子）。
- `|> within(month)`——月の窓でパーティション化。点は捨てられず、窓に「所属」する。
- `|> first`——**各窓の**先頭点を選ぶ。選択子は窓相対——「全体の最初の 1 点」ではない。

これで「毎月 1 日」になる。`last` なら月末、`nth(25)` なら各月 25 日。**評価範囲は右開区間**
（`..2026-04-01` は 4 月を含まない）にも注意。

- 参照: [reference/within](https://kairos-lang.org/reference/within.html)・[reference/first](https://kairos-lang.org/reference/first.html)
- **演習**: [L3-001 各月の先頭日](../L3/001-first-of-month.md)

## 第 4 章 前提が意味を変える——premise の読み方

第 1 章の主張の実演。次の式は「毎週の先頭日」だが、**何曜日が発火するかは式に書かれていない**:

```kairos
everyDay |> within(week) |> first
```

答えは前提の `wkst:`（週の始まり）が握っている——`wkst: Mon` なら月曜列、`wkst: Sun` なら
日曜列。式だけ読んで答えた気になるのが 3 級で最も落ちやすい罠で、**premise 層を読んでから
本体を読む**という順序が読解の型になる。tz・calendar-system も同じ構図（どの暦法の「月」か、
どの壁時計の「一日」か）。

- 参照: [仕様 §3.6（week と wkst）](https://kairos-lang.org/spec/20-premise-layer.html)
- **演習**: [L3-004 週窓と wkst](../L3/004-week-wkst.md)

## 第 5 章 結合子と filter——組み合わせを読む

複数の列は結合子で合成される: `A | B`（和）・`A & B`（積）・`A \ B`（差）。営業日の定番形
`everyDay \ (satSun | holidays)` は「全日から、土日と祝日の和を引く」と**内側から**読む。

`filter` は条件で間引く: `everyDay |> filter(d => weekday(d) == Fri)` は金曜だけの列。
`weekday(d)` は「点 d の属する窓のラベルを読む」形（束縛名射影）で、3 級では「d の曜日」と
読めれば足りる。

パイプの**順序が意味を決める**ことも覚えておく——`filter |> within |> nth(2)`（金曜に濾して
から各月 2 本目＝第 2 金曜）と `within |> nth(2) |> filter`（各月 2 日のうち金曜の日だけ）は
別物。この罠の実例は 2 級で扱う。

- 参照: [仕様 §4.5（結合子）](https://kairos-lang.org/spec/30-body-layer.html)・[reference/filter](https://kairos-lang.org/reference/filter.html)
- 演習: 本章の演習問題は次回の公開分で追加予定。

## 巻末——3 級の到達確認

1. premise 層と本体層の役割を人に説明できる（第 1 章）
2. 日付・幅リテラルの合法／字句エラーを判別できる（第 2 章）
3. 「生成→窓→選択」のパイプを声に出して読める（第 3 章）
4. 式を読む前に前提を読む習慣がある（第 4 章）
5. 結合子と filter の合成を内側から分解できる（第 5 章）

サンプル問題 4 問（各章末）を自力で解けたら、3 級の水準はほぼ届いている。実行環境で確かめたい
ときは各問題末尾の「検証」節を[リファレンス実装](https://github.com/azathothx/kairos-lang)で
実行すればよい。
